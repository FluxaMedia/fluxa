# Desktop platform shell

This directory contains Fluxa’s Windows, macOS, Linux, browser, and webOS client shell.

For the project overview and contribution rules, see the repository [README](../../README.md) and [CONTRIBUTING](../../CONTRIBUTING.md).

Common local checks are run from the repository root:

```bash
npm run check:desktop
npm run test:desktop
```
